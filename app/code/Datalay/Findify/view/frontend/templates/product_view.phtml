<?php
$product = $block->getCurrentProduct();
if($product && $product->getId()) :
	$pid = $product->getId();
?>
    <div data-findify-event="view-page" data-findify-item-id="<?php echo $pid; ?>"></div>

<?php endif; ?>
